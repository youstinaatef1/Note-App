const Note = require("../models/Note");
const User = require("../models/User");

const postNoteController = async (req, res) => {
    try {
        console.log("Note Endpoint")
    } catch (error) {
        
    }
};
module.exports = {postNoteController};